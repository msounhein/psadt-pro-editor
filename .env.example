# Database Configuration
# For SQLite local development (recommended for local setup)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (production)
# DATABASE_URL="postgresql://username:password@localhost:5432/psadt_pro?schema=public"

# NextAuth Configuration
# Generate a secure secret with: openssl rand -base64 32
# or use a random string generator
NEXTAUTH_SECRET="your-secure-nextauth-secret-key"
NEXTAUTH_URL="http://localhost:3000"

# File Storage Paths (for PSADT packages and files)
STORAGE_PATH="./storage"
TEMPLATES_PATH="./templates"
PSADT_PATH="./psadt"

# Optional: Configure email (for production)
# EMAIL_SERVER=smtp://username:password@smtp.example.com:587
# EMAIL_FROM=noreply@example.com

# JWT Secret for NextAuth.js (used for authentication)
# In production, use a secure random string
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# File storage configuration
FILE_STORAGE_PATH="./storage"

# For production environments, consider using a cloud storage service like S3
# AWS_S3_BUCKET="your-s3-bucket-name"
# AWS_S3_REGION="your-s3-region"
# AWS_ACCESS_KEY_ID="your-access-key"
# AWS_SECRET_ACCESS_KEY="your-secret-key"

# GitHub API configuration
# Create a personal access token at https://github.com/settings/tokens
# Required for higher rate limits when downloading templates
# GITHUB_TOKEN="your-github-personal-access-token"

# Node environment
NODE_ENV="development" 